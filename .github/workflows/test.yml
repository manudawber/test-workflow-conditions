name: Testing

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - src.py

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - run: echo "No testing required"
